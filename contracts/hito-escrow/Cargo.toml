[package]
name = "hito-escrow"
version = "0.1.0"
edition = "2021"
rust-version = "1.84.0"
license = "MIT"
publish = false
[lib]
crate-type = ["cdylib", "rlib"]
[dependencies]
soroban-sdk = { workspace = true }
[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
